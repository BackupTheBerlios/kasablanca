v 0.3.1

-add fxp overwrite and resume.
-change colouring for left and right
-repair spaces xfer bug, changes in kbftp probably, local files are sent to the client using the path to the file. if there are spaces in it it will fail.
-slow down update cycle, transfer info should only be refreshed every 100msec or something.

v 0.4

-recursive delete
-add alternative fxp
-define standard directory in bookmarks
-update FormatFileList(), make it use qt stuff instead of c
-remember dirlistings (caching)
-move tasks in taskview
-auto-adjustment for the header
-check if filesize variables are ok

v 0.5

-add owner and rights files to browser view
-choose port range
